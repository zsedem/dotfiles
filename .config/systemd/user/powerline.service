[Unit]
Description=Powerline daemon as a system service
After=local-fs.target

[Service]
Type=simple
ExecStart=/usr/bin/powerline-daemon --foreground
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
